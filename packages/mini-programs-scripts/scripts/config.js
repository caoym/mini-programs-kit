const srcRoot = 'src'
const pagePath = 'pages'
const componentPath = 'components'
const assetsPath = 'assets'

module.exports = {
  srcRoot,
  pagePath,
  componentPath,
  assetsPath,
  entriesPaths: [
    pagePath,
    componentPath,
  ]
}
